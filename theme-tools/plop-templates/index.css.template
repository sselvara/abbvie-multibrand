@import '{{#if isBrandTheme}}../../{{else if isBrandCSS}}../{{else}}../../{{/if}}{{blockName}}.css';

/* Brand-specific overrides for {{name}} block */

/* .{{name}} {
  h1 {
    background-color: red;
  }


} */
